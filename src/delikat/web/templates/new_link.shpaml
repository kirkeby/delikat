html
  > xi:include href=page.shpaml
  head
    title | del.ik.at -- new link
  body
    h1 | new link
    h2 py:if=defined('notice') | ${notice}
    form method=POST action=${url_for('new_link')}
      fieldset
        label
          .b | url:
          > input name=url
        label
          .b | title:
          > input name=title
        label
          .b | tags:
          > input name=tags

      fieldset
        > input type=submit value="make it so!"
